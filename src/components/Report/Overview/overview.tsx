import { Box, BoxProps } from '@chakra-ui/react'
import React from 'react'

const OverviewHeaderBox = (properties: BoxProps) => (
  <Box borderWidth="1px" borderColor="blue.100" {...properties} />
)

export default OverviewHeaderBox
