query GET_COMPANY_TEAMS {
  teams(filters: { onlyCompanies: true }) {
    id
    teamsRanking {
      id
      name
      progressIncreaseSinceLastWeek
      status {
        progress
        confidence
      }
    }
  }
}
