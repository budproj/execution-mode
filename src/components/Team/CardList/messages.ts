import { defineMessages } from 'react-intl'

type TeamCardMessage = 'loadMore'

export default defineMessages<TeamCardMessage>({
  loadMore: {
    defaultMessage: 'Carregar Mais',
    id: 'DaEVzP',
    description: 'Button title to load more rows',
  },
})
