import { defineMessages } from 'react-intl'

type KeyResultsSectionTimelineContentMessage = 'emptyState'

export default defineMessages<KeyResultsSectionTimelineContentMessage>({
  emptyState: {
    defaultMessage: 'Este resultado-chave não possui nenhum check-in',
    id: 'NF2bAx',
    description: 'This text is displayed in our key result timeline when no content exist',
  },
})
