import { defineMessages } from 'react-intl'

type KeyResultsSectionObjectiveMessage = 'emptyState'

export default defineMessages<KeyResultsSectionObjectiveMessage>({
  emptyState: {
    defaultMessage: 'Tudo tranquilo por aqui!',
    id: 'F7vED6',
    description: 'The emptyState message.',
  },
})
