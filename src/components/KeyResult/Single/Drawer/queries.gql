query GET_KEY_RESULT($id: ID!) {
  keyResult(id: $id) {
    id
    title
    description
    goal
    owner {
      name
    }
    objective {
      title
      cycle {
        dateStart
        dateEnd
        createdAt
      }
    }
    progressReports(limit: 1) {
      id
      valueNew
    }
    confidenceReports(limit: 1) {
      id
      valueNew
    }
  }
}
