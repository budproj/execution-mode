import { defineMessages } from 'react-intl'

type UpdatePictureModalMessage = 'submitButtonLabel'

export default defineMessages<UpdatePictureModalMessage>({
  submitButtonLabel: {
    defaultMessage: 'Finalizar',
    id: '3e+zPY',
    description: 'This button is displayed with our crop picture modal to submit the form',
  },
})
