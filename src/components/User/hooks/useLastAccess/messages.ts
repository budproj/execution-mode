import { defineMessages } from 'react-intl'

type useLastAccessMessages = 'todayLastAccess'

export default defineMessages<useLastAccessMessages>({
  todayLastAccess: {
    defaultMessage: 'Hoje',
    id: 'Yc4eh9',
    description: 'This message appears if the user last access is today.',
  },
})
