import { defineMessages } from 'react-intl'

type CreateUserFormMessage = 'firstNameLabel'

export default defineMessages<CreateUserFormMessage>({
  firstNameLabel: {
    defaultMessage: 'Nome',
    id: 'V4ahTq',
    description: 'The label of the first option in the create user form',
  },
})
