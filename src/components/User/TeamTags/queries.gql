mutation REMOVE_TEAM_FROM_USER($userID: ID!, $teamID: ID!) {
  removeTeamFromUser(userID: $userID, teamID: $teamID) {
    id
    teams {
      edges {
        node {
          id
          name
        }
      }
    }
  }
}
