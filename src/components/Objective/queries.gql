query GET_OBJECTIVE_KEY_RESULTS($objectiveID: ID!) {
  objective(id: $objectiveID) {
    id
    keyResults {
      title
      format
      currentConfidence
      currentProgress
      owner {
        name
        picture
      }
      team {
        name
      }
    }
  }
}
